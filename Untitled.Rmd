---
title: "Untitled"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.


```{r}
library(jsonlite)
fromJSON("https://data.tainan.gov.tw/dataset/4cd9345a-04bf-4328-a97d-314ff78b4763/resource/afc025fc-1ef4-447c-b785-a680391d0ca1/download/tnsport2.json", simplifyDataFrame = F) -> tainanSportsMap
```


```{r}
tainanSportsMap[[1]]
```

```{r}
listExample <- 
  list(
    student="小明",
    `學期`=list(
      `108-1`=list(
        list(
          name="個體經濟學",
          teacher="Alice",
          grade=85
        )
      ),
      `108-2`=list(
        list(
          name="總體經濟學",
          teacher="Mark",
          grade=78
        )
      )
    )
  )
# 有多少位置可選：
length(listExample)
```

```{r}
str(listExample)
```


```{r}
fromJSON("http://data.ntpc.gov.tw/api/v1/rest/datastore/382000000A-000157-002",
         simplifyDataFrame = F) -> newTaipeiCityVaccine
```


```{r}
newTaipeiCityVaccine[["result"]]
```

```{r}
str(newTaipeiCityVaccine[[1]])
```

```{r}
str(newTaipeiCityVaccine$result)
```
```{r}
str(newTaipeiCityVaccine[[2]],max.level = 1)
```
```{r}
listExample[c("student")]
```

```{r}
fromJSON("https://www.dropbox.com/s/qnm9bnz7vccrvcz/taipeiNightMarkets.json?dl=1", simplifyDataFrame = F) -> taipeiNightMarkets
```

```{r}
str(taipeiNightMarkets,max.level = 2)
taipeiNightMarkets[c(1,5,9)]
```

```{r}
names(taipeiNightMarkets)
```

```{r}
c(T,F,F,F,T,F,T,T,T,F,F,F,F,F) -> selectMarkets
```

```{r}
str(taipeiNightMarkets[c(T,F,F,F,T,F,T,T,T,F,F,F,F,F)],max.level = 1)
```

```{r}
str(taipeiNightMarkets[selectMarkets],max.level=1)
```

```{r}
selectMarkets[c(2,6,8)] <- TRUE
```

```{r}
str(taipeiNightMarkets[selectMarkets],max.level = 1)
```

```{r}
majors <- c("經濟學系","經濟學系","社會學系","社工系","經濟學系")
names <- c("小英", "小美", "大雄","阿華", "阿尼")
gender <- c("F","F","M","F","M")
```

```{r}
econOnly <- c(T,T,F,F,T); econOnly
names[econOnly]
gender[econOnly] <- c(T,T,F)

econFemaleOnly <- gender[econOnly]; econFemaleOnly

FinalResult <- names[econOnly][c(T,T,F)]; FinalResult
```










